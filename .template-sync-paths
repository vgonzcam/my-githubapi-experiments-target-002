# .template-sync-paths
# List of files or directories to sync from the template repository.
# All paths are relative to the root of the repository.
# Lines starting with '#' are comments and ignored.
# Blank lines are ignored.

# To sync the entire repository (except exclusions), use '.'
.

# To sync only the shared folder, use 'shared/'
# shared/

# Add other files or folders to sync as needed, one per line.